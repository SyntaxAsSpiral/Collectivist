# LLM Provider Configuration
# Choose one: lmstudio, ollama, openrouter, pollinations, anthropic, openai, custom

LLM_PROVIDER=lmstudio

# API Key (required for cloud providers: openrouter, anthropic, openai)
# LLM_API_KEY=your_api_key_here

# Custom Base URL (optional, overrides default for provider)
# LLM_BASE_URL=http://localhost:1234/v1
